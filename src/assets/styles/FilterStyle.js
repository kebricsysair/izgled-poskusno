import { makeStyles } from '@mui/styles';

const filterUseStyles = makeStyles({
    formControl: {
       minWidth: "120px",
        fontSize: "16px"
    },
    inputLabel: {
        minWidth: "120px"
    },
    select: {
        minWidth: "140px"
    }
})
export default filterUseStyles;
